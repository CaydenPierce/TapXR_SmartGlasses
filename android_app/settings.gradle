pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Convoscope"
include ':app'

include ':tap-android-sdk'
project(':tap-android-sdk').projectDir = new File(rootProject.projectDir, '../tap-android-sdk/tap-android-sdk')

include ':SmartGlassesManager'
project(':SmartGlassesManager').projectDir = new File(rootProject.projectDir, '../SmartGlassesManager/SGM_android/SmartGlassesManager')